# specify path to goacoin.conf or leave blank
# default is the same as GoacoinCore
#goacoin_conf=/home/evan82/.goacoincore/goacoin.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
